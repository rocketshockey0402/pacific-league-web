// ──────────────────────────────────────────────
//  퍼시픽 리그 자동화 설정
//  운영진이 바꿀 수 있는 값은 이 파일에 모아두었습니다.
// ──────────────────────────────────────────────

// 승점 계산 기준  ※ [확인 필요] — 리그 규정 확정 후 숫자만 바꾸세요.
//  예) 정규승 3 / 무승부 1 / 패 0
export const POINTS = {
  win: 3,
  draw: 1,
  loss: 0,
};

// true  : 경기 결과 DB로 순위를 '자동 계산' (운영진은 결과만 입력하면 됨)  ← 권장
// false : 노션 '순위표' DB에 직접 입력한 값을 그대로 사용
export const AUTO_STANDINGS = true;
